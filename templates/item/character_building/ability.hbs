<!--TODO: Change this to a sheet that can be filled out based on Sequence's data model data -->
<div class="{{cssClass}} sequence-sheet-root">

  {{!-- Sheet Tab Navigation --}}
  <nav class="sheet-tabs tabs" data-group="sheet">
    <a class="item {{tabs.description.cssClass}}" data-tab="description" data-group="sheet" data-action="tab">Description</a>
    <a class="item {{tabs.details.cssClass}}" data-tab="details" data-group="sheet" data-action="tab">Details</a>
  </nav>

  {{!-- Sheet Body --}}
  <section class="sheet-body">

    {{!-- Description Tab --}}
    <div class="tab description {{tabs.description.cssClass}}" data-group="sheet" data-tab="description">
      {{#if editable}}
        <prose-mirror name="system.description" button="true" editable="{{editable}}" toggled="false" value="{{system.description}}">
          {{{enrichedDescription}}}
        </prose-mirror>
      {{else}}
        {{{enrichedDescription}}}
      {{/if}}

    </div>

    {{!-- Details Tab --}}
    <div class="tab details {{tabs.details.cssClass}}" data-group="sheet" data-tab="details">

      <!--Sequence Number (sequence number - overrides lower sequences on actor sheets)-->
      {{!-- <div class="sequenceNumber flexrow">
        <label>Sequence Number</label>
        <input type="number" name="system.sequence_number" value="{{system.sequence_number}}" data-dtype="Number"/>
      </div> --}}

      <!--Acting Principals (string GM secret)-->
      {{!-- <fieldset class="actingPrincipals flexrow">
        <legend>Acting Principals</legend>
        {{#if editable}}
          <prose-mirror name="system.acting_principals" button="true" editable="{{editable}}" toggled="false" value="{{system.acting_principals}}">
            {{{enrichedActingPrincipals}}}
          </prose-mirror>
        {{else}}
          {{{enrichedActingPrincipals}}}
        {{/if}}
      </fieldset> --}}

      <!--Attribute Gain (list of attribute increases)-->
      {{!-- <fieldset class="attribute-gain">
        <legend>Attribute Gain</legend>
        <ol class="items-list">
          <li class="items-header flexrow">
            <div class="item-name">Attribute</div>
            <div class="item-prop">Value</div>
            <div class="item-controls"></div>
          </li>
          {{#each attributeGain as |row|}}
          <li class="item flexrow">
            <div class="item-name">
              <select name="system.attribute_gain.{{row.index}}.attribute_name">
                {{selectOptions ../attributeChoices selected=row.attribute_name localize=true}}
              </select>
            </div>
            <div class="item-prop">
              <input type="number" name="system.attribute_gain.{{row.index}}.value" value="{{row.value}}" data-dtype="Number"/>
            </div>
            <div class="item-controls">
              <a class="item-control" data-action="deleteAttributeGain" data-index="{{row.index}}" title="Delete">
                <i class="fas fa-trash"></i>
              </a>
            </div>
          </li>
          {{/each}}
        </ol>
        <a class="item-control add-row" data-action="addAttributeGain">
          <i class="fas fa-plus"></i> Add Attribute
        </a>
      </fieldset> --}}

      <!--Skill Increase -->
      {{!-- <fieldset class="skill-increase" data-drop-zone="skill_increase">
        <legend>Skill Increase</legend>
        <ol class="items-list">
          <li class="items-header flexrow">
            <div class="item-name">Skill</div>
            <div class="item-prop">Value</div>
            <div class="item-controls"></div>
          </li>
          {{#each skillIncrease as |row|}}
          <li class="item flexrow" data-uuid="{{row.uuid}}">
            <div class="item-name">
              <img class="item-image" src="{{row.img}}" alt="{{row.name}}"/>
              <h4>{{row.name}}</h4>
            </div>
            <div class="item-prop">
              <input type="number" name="system.skill_increase.{{row.index}}.value" value="{{row.value}}" data-dtype="Number"/>
              <input type="hidden" name="system.skill_increase.{{row.index}}.skill_uuid" value="{{row.uuid}}"/>
            </div>
            <div class="item-controls">
              <a class="item-control" data-action="deleteSkillIncrease" data-index="{{row.index}}" title="Delete">
                <i class="fas fa-trash"></i>
              </a>
            </div>
          </li>
          {{/each}}
          {{#unless skillIncrease.length}}
          <li class="item flexrow drop-hint">Drag a Skill</li>
          {{/unless}}
        </ol>
      </fieldset> --}}

      <!--Abilities Gained -->
      {{!-- <fieldset class="abilities-gained" data-drop-zone="abilities_gained">
        <legend>Abilities Gained</legend>
        <ol class="items-list">
          <li class="items-header flexrow">
            <div class="item-name">Ability</div>
            <div class="item-controls"></div>
          </li>
          {{#each abilitiesGained as |row|}}
          <li class="item flexrow" data-uuid="{{row.uuid}}">
            <div class="item-name">
              <img class="item-image" src="{{row.img}}" alt="{{row.name}}"/>
              <h4>{{row.name}}</h4>
            </div>
            <div class="item-controls">
              <a class="item-control" data-action="deleteAbilityGained" data-index="{{row.index}}" title="Delete">
                <i class="fas fa-trash"></i>
              </a>
            </div>
          </li>
          {{/each}}
          {{#unless abilitiesGained.length}}
          <li class="item flexrow drop-hint">Drag an Ability</li>
          {{/unless}}
        </ol>
      </fieldset> --}}

    </div>

  </section>

</div>
